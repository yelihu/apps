server:
  port: 8082

spring:
  application:
    name: app2

management:
  server:
    port: 8082
  endpoints:
    jmx:
      exposure:
        include: "*"
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always 